START
DEF A:NUMBER; B:NUMBER; C:NUMBER; RESULT:NUMBER
READ 2A
READ B
READ C
IF A === B && A === C STARTIF ASSIGN RESULT:A FINISHIF
IF A === B && C > A STARTIF ASSIGN RESULT:C FINISHIF
IF B === C && A > B STARTIF ASSIGN RESULT:A FINISHIF
IF A === C && B > A STARTIF ASSIGN RESULT:B FINISHIF
IF A > B && A > C STARTIF ASSIGN RESULT:A FINISHIF
IF B > A && B > C STARTIF ASSIGN RESULT:B FINISHIF
IF C > A && C > B STARTIF ASSIGN RESULT:C FINISHIF
PRINT("The minimum value is " + RESULT)
FINISH
